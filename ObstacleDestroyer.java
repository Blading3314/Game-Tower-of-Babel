import greenfoot.*;

public class ObstacleDestroyer extends Actor {
    public ObstacleDestroyer() {
        GreenfootImage img = new GreenfootImage(900, 10); // Full width, small height
        img.setTransparency(0); // Make it invisible
        setImage(img);
    }

    public void act() {
        // This actor does not need to do anything actively
    }
}
